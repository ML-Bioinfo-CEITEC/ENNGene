import sys

from ..utils.subcommand import Subcommand


class BatchCommand(Subcommand):

    def __init__(self):
        # define here the generic options for different batch runs

    def run(self):
        # run batch of selected subcommands
